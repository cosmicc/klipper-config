[Variables]
active_spool = 'S2'
extra_weight = 1158.0  # Extra weight in grams from any non spool/filament weight on scale like a filament dryer.  This is torn off of the total weight.
distance_to_extruder = 900  # Distance in mm from spool to extruder gears (not hotend).  Added to weighted length as this is off the spool and cant be weighed.
calibration_weight = 5  # Calibration weight used for the test tool in grams
calibration_offset = 0  # Set automatically by the calibration tool
